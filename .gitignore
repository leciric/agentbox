/agentbox
/bin/
# No trailing slash: that would match only directories, and a symlink named
# node_modules is a file to git (#84 committed one pointing into /tmp).
node_modules
/desktop/out/
/desktop/dist/

# Run logs, screenshots and video from real sessions: regenerated by
# scripts/demo/, kept out of the repo because they carry weight nobody
# reviews and can leak tokens, account names and home paths.
/.demo-runs/

# scripts/record/ still writes its own output here.
/docs/implementation/evidence/
